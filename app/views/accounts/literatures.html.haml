=render 'nav'
-unless @literatures.blank?
  %table.zebra-striped
    %tr
      %th Title
      %th Created
      %th
    -@literatures.each do |l|
      %tr
        %td=link_to l.title, l
        %td=dt l.created_at
        %td
          .pull-right
            =link_to "update", [:edit, l], :class => 'btn small'
            =link_to "delete", l, :method => :delete, :confirm => "Are you sure you want to delete '#{h l.title}'?", :class => 'btn small danger'
-else
  %p You haven't submitted anything yet. 