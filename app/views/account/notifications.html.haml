%table{style: 'width: 100%'}
  -@notifications.each do |notification|
    %tr
      %td.notification{class: notification.template}=notification.to_html
      %td=link_to "X", notification, class: 'btn btn-danger btn-mini', method: :delete, confirm: "Are you sure?"
=paginate @notifications