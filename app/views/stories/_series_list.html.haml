:ruby
  # required: @story or [series, current_story]
  if @story
    series = @story.series
    current_story = story
  end

%h3
  %small Series:
  =link_to series.title, series
%ol
  -series.stories.visible.each do |story|
    %li
      -if current_story == story
        %strong=sicon_link :story, story.title, story
      -else
        =sicon_link :story,  story.title, story

