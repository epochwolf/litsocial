=link_to "New Message", new_message_path(return: here), class:'btn btn-mini btn-success pull-right'

%table.table
  %thead
    %tr
      %th To
      %th On
      %th Message
  %tbody
    -@messages.each do |message|
      %tr
        %td=user_link message.to
        %td=dt message.created_at
        %td=rich message.message