%h2 
  Stories
  -if user_signed_in?
    .pull-right
      =link_to "New Story", new_story_path, class:'btn btn-success btn-mini'
-if @stories
  %table.table.table-striped
    %tr
      %th Title
      %th Series
      %th Author
      %th Created
    -@stories.each do |s|
      %tr
        %td=link_to s.title, s
        %td
          -if s.series
            \##{s.series_position} of 
            %small Series:
            #{link_to s.series.title, s.series}
          -else
            %em None
        %td=dt s.created_at
        %td=user_link s.user

  =paginate @stories
-else
  %p Nothing has been submitted yet.