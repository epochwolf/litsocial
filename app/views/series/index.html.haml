%h2 Series
-if @series
  %table.table.stories
    %thead
      %tr
        %th Title
        %th.hidden-phone # of Stories
        %th Author
        %th.hidden-phone Created
    %tbody
      -@series.each do |s|
        %tr
          %td=sicon_link :series, s.title, s
          %td.hidden-phone=s.stories_count
          %td=user_link s.user
          %td.hidden-phone=dt s.created_at

  =paginate @series
-else
  %p Nothing has been submitted yet.