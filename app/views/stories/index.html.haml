%h2 
  Stories
  -if user_signed_in?
    .pull-right
      =link_to "New Story", new_story_path, class:'btn btn-success btn-mini'
-if @stories
  %table.table.table-striped
    %tr
      %th Title
      %th Series
      %th Author
      %th Created
    -@stories.each do |s|
      %tr
        %td=icon_link "book", s.title, s
        %td
          -if s.series
            \##{s.series_position} of #{icon_link "list", s.series.title, s.series}
          -else
            %em None
        %td=dt s.created_at
        %td=link_to s.user.name, s.user

  =paginate @stories
-else
  %p Nothing has been submitted yet.