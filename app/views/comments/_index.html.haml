-# required: commentable

.comments-form=render "comments/new", comment: commentable.comments.build
.comments
  -commentable.comments.each do |comment|
    -next if comment.new_record?
    =render 'comments/show', comment: comment